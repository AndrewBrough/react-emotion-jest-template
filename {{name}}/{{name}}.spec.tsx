import * as React from "react";

import { mount } from "enzyme";
import { {{name}} } from "../{{name}}";

describe("{{name}}", () => {
  let wrapper;

  beforeEach(()=>{
    wrapper = mount(
      <{{name}} />
    );
  })
});
